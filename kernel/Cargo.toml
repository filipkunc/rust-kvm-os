[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
libm = "0.2.16"
shared = { path = "../shared" }

[profile.release]
panic = "abort"
lto = true

[profile.dev]
panic = "abort"
