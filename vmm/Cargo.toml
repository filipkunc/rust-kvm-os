[package]
name = "vmm"
version = "0.1.0"
edition = "2024"

[dependencies]
kvm-ioctls = "0.25"
kvm-bindings = "0.14"
object = { version = "0.39", default-features = false, features = ["read_core", "elf", "std"] }
libc = "0.2"
shared = { path = "../shared" }
winit = "0.30"
softbuffer = "0.4.8"
